---
title: Durable Workflow vs Temporal
description: "Compare Durable Workflow and Temporal for PHP and Laravel teams: embedded execution, managed capacity versus usage billing, operational ownership, and a tested PHP example."
image: /img/durable-workflow-2.0.png
---

import CodeBlock from '@theme/CodeBlock';
import handlers from '@site/static/examples/temporal-comparison/handlers.php';

# Durable Workflow vs Temporal

**Durable execution for teams that want to stay close to their Laravel application
and control their infrastructure budget.**

Durable Workflow lets you run workflows inside Laravel using your existing
database and queue workers, or connect PHP, Python, and Rust workers to a separate
runtime. Temporal uses a dedicated orchestration service, self-hosted or managed
by Temporal Cloud.

The choice comes down to where you want workflow state to live, what you want to
operate, and how you want your bill to grow. Durable Workflow is built around
**predictable cost, predictable behavior, and predictable operations**.

*By the Durable Workflow maintainers. Updated September 21, 2026.*

## Choose by the shape of your team

| Your priority | Consider Durable Workflow when... | Consider Temporal when... |
| --- | --- | --- |
| Predictable operations | You want workflows inside an existing Laravel app, using its database, queue workers, container, and deployment. | You want a separate orchestration service, managed by Temporal Cloud or operated by your platform team. |
| Predictable cost | You prefer a chosen Cloud capacity allocation and a monthly cap, accepting finite throughput and storage. | You prefer consumption billing tied to Actions and stored history, particularly when demand is intermittent. |
| Predictable behavior | You want Laravel-native workflow authoring, or PHP, Python, and Rust workers using a shared service-mode protocol. | Your application already uses Temporal's execution model, or needs its wider SDK selection and integrations. |

Those deployment choices are described in the
[Durable Workflow introduction](/docs/introduction/),
[Temporal Service architecture](https://docs.temporal.io/temporal-service), and
[Temporal SDK guides](https://docs.temporal.io/develop).
## Keep workflows inside Laravel

If your team already operates Laravel, embedded Durable Workflow puts workflow
execution in that application. Workflow and activity classes use Laravel's
container, state lives in your database, and queue workers execute the work.
You deploy and operate them with the rest of your app, without adding a separate
orchestration service. Start with the [embedded quickstart](/docs/quickstart/).

[Temporal's PHP workers](https://docs.temporal.io/develop/php) instead connect
to Temporal Service. That separation fits teams that want orchestration managed
independently of their application.

Durable Workflow also supports that deployment model. The
[PHP SDK](https://php.durable-workflow.com/) connects to self-hosted Server or
Cloud, and its Laravel bridge preserves container integration, configuration,
and logging. Service mode uses the SDK API rather than the embedded API.

## Predictable cost: capacity or consumption?

[Durable Workflow Cloud pricing](https://cloud.durable-workflow.com/pricing)
charges for provisioned runtime time, with an active-month minimum and a UTC
monthly cap. Starts, activity attempts, timers, signals, and queries do not add
per-action charges. They still consume the plan's resources. Storage is fixed
per plan; needing more means arranging a whole-plan upgrade, not an automatic
storage overage. Customer worker infrastructure is separate.

[Temporal's current pricing](https://temporal.io/pricing) has **no base monthly
fee** on its entry-level pay-as-you-go offer. Its bill combines Actions, active
and retained storage, and support. The
[detailed pricing documentation](https://docs.temporal.io/cloud/pricing)
lists Developer support at 10% of usage. For an intermittent workload,
consumption billing can cost less than keeping dedicated capacity provisioned.

### What the bill looks like

All figures below are USD, before tax.

**Durable Workflow Cloud Dev:** the published rate is $0.075/hour, a $2 minimum
per active UTC month, and a $50 monthly cap. For one unchanged provisioned
runtime in one UTC month:

```text
No provisioned time: $0
10 billable hours: min($50, max($2, 10 × $0.075)) = $2
100 billable hours: min($50, max($2, 100 × $0.075)) = $7.50
720 billable hours: min($50, max($2, 720 × $0.075)) = $50
```

Dev includes 5 GB of storage and has **no uptime SLA**. Provisioned idle time is
still billed. Deprovisioning ends capacity accrual; it does not undo a minimum
already incurred. See the [billing and plan terms](/docs/polyglot/cloud-control-plane/)
for other plans and lifecycle rules.

**Temporal Developer:** for an example month, assume 1 million
billable Actions, 720 GB-hours of active storage and 720 GB-hours of retained
storage. Storage is measured separately from Actions. At the documented rates:

```text
Actions:          1 × $50                      = $50.00
Active storage:   720 × $0.042                  = $30.24
Retained storage: 720 × $0.00105                =  $0.756
Usage subtotal:                                = $80.996
Developer support: 10% × $80.996               =  $8.0996
Total, rounded to cents:                       = $89.10
```

The Temporal calculation excludes credits, optional features, and replicated
namespaces. Both examples exclude customer worker costs. They illustrate billing
models, not equivalent capacity: Temporal's entry-level offer includes an SLA,
while Cloud Dev does not. Size either service against your workload.

### A workflow is not one Action

Temporal documents [billable Actions](https://docs.temporal.io/evaluate/cloud/actions)
including workflow starts, activity starts and retries, timers, signals, and
queries. Replaying workflow history is not itself billed as new Actions.
A successfully started workflow with one successfully executed ordinary activity
and no additional billable operations contributes two Actions, not one. A retry
can change that count; stored history is another billing dimension.

With Durable Workflow Cloud, those operations use your existing allocation
without adding per-action charges. You control spend through provisioned time,
plan size, and retention. More execution or storage requires enough capacity to
support it.

## Predictable behavior: workflow code and activity effects

Both projects separate workflow decisions from activity execution. Activities
can be retried, so make external effects such as payments idempotent. See our
[determinism and idempotency guidance](/docs/constraints/idempotent-vs-deterministic/)
and Temporal's [Activity documentation](https://docs.temporal.io/activities).

This **PHP service-mode SDK** workflow schedules a greeting activity and returns
its result. Download the same handler file and run its test below.

<CodeBlock language="php" title="handlers.php">{handlers}</CodeBlock>

Get the [complete example files](https://github.com/durable-workflow/durable-workflow.github.io/tree/main/static/examples/temporal-comparison)
and run them in that directory with PHP 8.1+ and Composer:

```sh
composer install
php test.php
```

The local test checks registration, activity scheduling and execution, and replay
with a recorded activity result:

```text
PASS: registration, activity scheduling, activity result, and completed-history replay.
```

For an end-to-end run, start the [local Server](/docs/polyglot/server/) at
`http://localhost:18080` with its documented development token, then run
`php worker.php` and `php client.php` in separate terminals. The result is
`{"greeting":"Hello, Ada!"}`. This example is tested against published packages
and Server; [test results and versions](https://github.com/durable-workflow/durable-workflow.github.io/pull/135)
are recorded with the source.

For Cloud, supply your runtime URL, namespace, and separate client and worker
tokens. The [PHP quickstart](https://php.durable-workflow.com/getting-started/first-workflow/)
covers the connection details.

## Predictable operations: know what you own

| Deployment | What you operate |
| --- | --- |
| Durable Workflow embedded Laravel | Laravel app, queue consumers, database, and the associated backups and monitoring. |
| Durable Workflow self-hosted Server | Server, its database/cache and operational processes, plus your SDK clients and workers. |
| Durable Workflow Cloud | Your application clients and workers; we operate the selected managed runtime. |
| Temporal self-hosted | Temporal Service and its persistence/visibility configuration, plus your application workers. |
| Temporal Cloud | Your application workers and clients; Temporal operates its managed service. |

Compare the [Server deployment guide](/docs/polyglot/server/),
[Cloud ownership boundary](/docs/polyglot/cloud-control-plane/), and Temporal's
[self-hosting guide](https://docs.temporal.io/self-hosted-guide) and
[Cloud overview](https://docs.temporal.io/cloud).
Both managed services leave your application workers in your hands. For
self-hosting, plan for database recovery, upgrades, monitoring, and the
availability your application needs. Embedded Durable Workflow keeps that work
inside your existing Laravel operations; a separate service gives it an
independent deployment and lifecycle.

## When to choose which

**Choose Durable Workflow** to add durable execution to an existing Laravel app,
or to run PHP, Python, and Rust workers against capacity you choose and budget
for. Its embedded path keeps operations close to the application; its Cloud
plans keep normal execution behavior out of per-action billing.

**Choose Temporal** when its wider SDK ecosystem, integrations, and your team's
existing expertise are the better fit, or when paying for consumed Actions and
storage suits your workload better than provisioning capacity.

**Next:** [try embedded Laravel](/docs/quickstart/),
[run the PHP SDK example](https://php.durable-workflow.com/getting-started/first-workflow/),
or [compare managed capacity plans](https://cloud.durable-workflow.com/pricing).
For the other side, start with [Temporal's PHP guide](https://docs.temporal.io/develop/php).

Found a factual error? [Open a documentation issue](https://github.com/durable-workflow/durable-workflow.github.io/issues/new)
with the claim and a current source. We will correct the page when the evidence changes.
